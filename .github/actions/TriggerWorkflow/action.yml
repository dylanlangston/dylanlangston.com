name: 'Trigger another Workflow'
description: 'Triggers a another workflow'
inputs:
  runId:
    description: 'Current Workflow Run ID.'
    required: true
    type: string
  token:
    description: 'Personal Access Token'
    required: true
    type: string
  trigger:
    description: 'Workflow trigger'
    required: true 
    type: string
runs:
  using: "node20"
  main: "main.mjs"
branding:
  icon: 'play'
  color: 'green'